# Contributing

Keep changes scoped, typed, tested, and reproducible. Never commit secrets or fabricated benchmark results. Run `ruff check .`, `mypy app`, and `pytest` from `backend/` before opening a pull request.
